
services:
  aichat:
    image: ghcr.io/iankulin/aichat
    container_name: aichat
    env_file:
      - .env
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
    restart: unless-stopped