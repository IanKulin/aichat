{
  "name": "aichat",
  "version": "0.0.1",
  "description": "Chat interface to LLMs",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "npx eslint *.js lib/*.js --fix --max-warnings 0",
    "format": "npx prettier *.js lib/*.js --write"
  },
  "author": "IanKulin",
  "license": "MIT",
  "dependencies": {
    "dotenv": "^16.5.0",
    "express": "^5.1.0"
  },
  "devDependencies": {
    "eslint": "^9.0.0",
    "prettier": "^3.0.0"
  }
}
